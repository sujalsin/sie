from __future__ import annotations

from typing import Any


def ingest_submission(source: str, payload: dict[str, Any] | None = None) -> dict[str, Any]:
    """Acquire a submission from the given source and return a normalized raw record."""
    return {}
